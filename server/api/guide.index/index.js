"user strict";
const express=require("express");
const router=express.Router();
const controller = require('./guide.controller.js');


// router.get('/doLogin', controller.doLogin);

module.exports = router;
